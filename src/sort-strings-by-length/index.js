/**
 * Sorts array of strings by shortest to longest
 *
 * @param {Array<string>} strings
 *
 * @return {Array<string>}
 */
export default function sortStringsByLength (strings = [], descending = false) {}
