/**
 * Sorts array of strings by shortest to longest
 *
 * @param {Array<string>} strings
 *
 * @return {Array<string>}
 */
export default function sortByStringLength (strings = [], descending = false) {}
